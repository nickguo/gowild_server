<img src="/images/manage_interests_penalties.jpg" style="width:800px">
<br></br>

<div class="foreground_content">
    <%= form_for current_user do |f| %>
        <%= f.select :from_account, Account.where("closed = false").collect {|account| [sprintf("%d - %s - %s - $%.2f", account.account_number,account.account_type,account.owner, account.balance), account.id]}, :prompt => "Select account to update interests/penalties for" %>

        <%= f.hidden_field 'id' %>
        <%= f.submit 'calculate interest' %>
        <script>
            $("#from_email").change( function() {
                var selectopt = $(this.children[this.selectedIndex]);
            });
        </script>
    <% end %>
</div>
